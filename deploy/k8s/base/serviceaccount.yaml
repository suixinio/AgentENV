# 阶段 4/Stage E: the `agentenv-scheduler` ServiceAccount this file used to
# also declare (Stage A of the scheduler fold,
# docs/proposals/_sd-phase4-stageA-node-inventory.md) is deleted along with
# the Go scheduler — no Pod references it any more. `agentenv-api` keeps its
# own identity below.
apiVersion: v1
kind: ServiceAccount
metadata:
  name: agentenv-api
