# Not listed in kustomization.yaml on purpose: applying it would overwrite the
# operator-filled Secrets on every `apply -k`. It documents the three Secrets
# the egress broker, the nodes and the api half read, and how to mint their
# contents.
#
#   NS=agentenv-system
#
#   # 1. The CA whose root guests trust for intercepted names. The broker
#   #    signs leaves under it, and each node's `aenv-api` issues that node a
#   #    short-lived intermediate from the same root. What bounds the leaves
#   #    today is the broker: it signs only a name a rule already matched,
#   #    under a per-sandbox mint budget (`ca.mints_per_sandbox_per_minute`).
#   #    The intermediates carry name constraints of their own; nothing this
#   #    root signs needs a `*.svc` name, so it may carry them too:
#   #    `-addext "nameConstraints=critical,excluded;DNS:svc,excluded;DNS:cluster.local"`
#   openssl ecparam -name prime256v1 -genkey -noout -out ca.key
#   openssl req -x509 -new -key ca.key -sha256 -days 3650 -subj "/CN=AgentENV Egress Leaf CA" \
#     -addext "basicConstraints=critical,CA:TRUE" \
#     -addext "keyUsage=critical,keyCertSign,cRLSign" \
#     -out ca.crt
#   kubectl -n $NS create secret generic egress-ca --from-file=ca.crt --from-file=ca.key
#
#   # 2. The master key values are encrypted under, and the bearer the broker
#   #    presents at the api half's resolve endpoint. Both are files, and the
#   #    key is in a Secret of its own: whoever holds a dump of `secret_values`
#   #    must not find the key beside it, so it does not go in
#   #    `agentenv-postgres`.
#   #
#   # 🔴 Losing this key loses every stored value. A database backup does not
#   # contain it and cannot recover it; that is the point of storing them
#   # apart. Back it up wherever the cluster's other root material lives.
#   head -c 32 /dev/urandom | base64 -w0 > key
#   kubectl -n $NS create secret generic agentenv-secrets-key --from-file=key
#   head -c 32 /dev/urandom | base64 -w0 > token
#   kubectl -n $NS create secret generic egress-resolver --from-file=token
#   # The broker reads that same token from `[resolver].token_file` and needs
#   # `[resolver].url` pointing at the api half, which is a DaemonSet env var:
#   #   AENV_EGRESS_RESOLVER_URL=http://agentenv-api:8000/internal
#   # `aenv-egress-networkpolicy.yaml` already admits that port.
#
#   # Then switch the nodes and the api half on. AENV_EGRESS_BROKER_MODE and
#   # AENV_SECRETS_BACKEND are both read once at process startup, and patching
#   # a ConfigMap changes no pod template (the generators run with
#   # `disableNameSuffixHash: true`), so each side needs its own restart.
#   kubectl -n $NS patch configmap egress-broker-config --type merge \
#     -p '{"data":{"AENV_EGRESS_BROKER_MODE":"local"}}'
#   kubectl -n $NS patch configmap secrets-store-config --type merge \
#     -p '{"data":{"AENV_SECRETS_BACKEND":"postgres"}}'
#   kubectl -n $NS rollout restart ds/agentenv-node deploy/agentenv-api
#   # 🔴 Rolling ds/agentenv-node destroys every sandbox on every node: the
#   # Firecracker processes are its children. Rolling ds/aenv-egress does not,
#   # which is why the broker is its own workload.
#
# Rotating the CA is appending the new certificate to `ca.crt` (nodes hand the
# whole bundle to guests) before any leaf is signed by it. Rotating the
# resolver bearer is a new random value in both `egress-resolver` and the api
# half's mount of it, `kubectl create secret --dry-run=client | kubectl apply
# -f -`, and a restart of the Pods that read it.
apiVersion: v1
kind: Secret
metadata:
  name: egress-ca
type: Opaque
stringData:
  # Mounted by the broker, which signs with it, and handed to guests by the
  # nodes as [egress_broker].guest_ca_cert_path — the certificate half only.
  ca.crt: "PEM certificate, see above"
  ca.key: "PEM private key, see above"
---
# The api Deployment projects both of these into /etc/agentenv/secrets; the
# broker mounts `egress-resolver` alone.
apiVersion: v1
kind: Secret
metadata:
  name: agentenv-secrets-key
type: Opaque
stringData:
  key: "base64 of 32 random bytes; losing it loses every stored value, see above"
---
apiVersion: v1
kind: Secret
metadata:
  name: egress-resolver
type: Opaque
stringData:
  token: "the bearer the broker presents at the api half's resolve endpoint, see above"
